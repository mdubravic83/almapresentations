# ══════════════════════════════════════════════
# SlideTranslate - Docker Compose for CloudPanel VPS
# ══════════════════════════════════════════════
#
# DEPLOY:
#   1. cp .env.example .env && nano .env  (add OPENAI_API_KEY)
#   2. docker compose up -d --build
#   3. In CloudPanel: create Reverse Proxy site → http://127.0.0.1:8090
#
# ══════════════════════════════════════════════

services:
  # ── MongoDB ──
  mongo:
    image: mongo:7
    restart: unless-stopped
    volumes:
      - mongo_data:/data/db
    networks:
      - app-network

  # ── Backend (FastAPI + Python) ──
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    restart: unless-stopped
    env_file: .env
    environment:
      - MONGO_URL=mongodb://mongo:27017
      - DB_NAME=slidetranslate
      - CORS_ORIGINS=*
    depends_on:
      - mongo
    networks:
      - app-network

  # ── Frontend (React → Nginx) ──
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
      args:
        - REACT_APP_BACKEND_URL=
    restart: unless-stopped
    ports:
      - "8090:80"
    depends_on:
      - backend
    networks:
      - app-network

volumes:
  mongo_data:

networks:
  app-network:
